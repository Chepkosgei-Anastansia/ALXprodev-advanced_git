# Git Flow Workflow

This repository follows the Git Flow branching model, using `master` for production-ready code and `develop` for ongoing development. Feature, release, and hotfix branches are created and merged according to Git Flow best practices to maintain a clean and structured commit history.
